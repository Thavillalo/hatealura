document.addEventListener("DOMContentLoaded", () => {
        const detalhesBtns = document.querySelectorAll(".detalhes-btn");

            detalhesBtns.forEach(btn => {
                    btn.addEventListener("click", () => {
                                alert("Detalhes do livro ainda não disponíveis.");
                                        });
                                            });
                                            });
